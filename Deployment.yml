apiVersion: v1
kind: Deployment
metadata:
  name: srm
  labels:
    app: srm-dev
spec:
  containers:
    - name: srm-dev-cont
      image: manikandan27/newimage:v1.latest
      ports:
        - containerPort: 8080
